# Breeze Siding Static Site Draft

This is a standalone static website draft for Breeze Siding LLC. It does not depend on WordPress.

Open `index.html` in a browser to preview it locally. The assets in `assets/images` are optimized copies made from the marketing folder; the original images were not changed.
