// Copy this file to config.js when connecting the dashboard to Supabase.
// Use the public anon key only. Never use a service-role key in website files.

window.BREEZE_CRM_CONFIG = {
  supabaseUrl: 'https://your-project.supabase.co',
  supabaseAnonKey: 'your-public-anon-key',
  mode: 'demo'
};
